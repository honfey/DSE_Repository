@model IEnumerable<SIS.Models.Course_Module>

@{
    ViewBag.Title = "Check Attendance Choices";
}

<h2>Check Attendance Choices</h2>

<br>

<table class="table">
    <tr>
        <th>
            <bold>Selection</bold>
        </th>

    </tr>
    @if (Model.ElementAtOrDefault(0).Status == false)
    {

    }
    else
    {
    <tr>
        <td>
            @Html.ActionLink("Mark Attendance (Today)", "MarkAttendance", new { id = Url.RequestContext.RouteData.Values["id"] }, new { onclick = "return confirm('Are you sure mark this class attendance ?');" })
        </td>
    </tr>
    }
    <tr>
        <td>
            @Html.ActionLink("Edit / Mark Back Attendance", "ChooseDate", new { id = Url.RequestContext.RouteData.Values["id"] })
        </td>
    </tr>

    <tr>
        <td>
            @Html.ActionLink("Delete Attendance", "ChooseDateDelete", new { id = Url.RequestContext.RouteData.Values["id"] })
        </td>
    </tr>

 

    <tr>
        <td>
            @Html.ActionLink("Check Individual Attendance (By Name)", "CheckIndividual", new { id = Url.RequestContext.RouteData.Values["id"] })
        </td>
    </tr>


    <tr>
        <td>
            @Html.ActionLink("Check All Attendance", "CheckAttendance", new { id = Url.RequestContext.RouteData.Values["id"] })
        </td>
    </tr>


</table>
