@model IEnumerable<SIS.Models.ModuleStandard>

@{
    ViewBag.Title = "Index";
}

<h2>ModuleStandard</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ClassStudent.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MarkType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mark)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ClassStudent.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MarkType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mark)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
@Html.ActionLink("MultipleEdit", "EditList")
